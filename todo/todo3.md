# todo3 — S3 OPTIONS modules (continuous)

<!-- Refill this file per-module as modules get picked from optionsmodules.md
     (its Planned list is the queue). Migration order first, then new modules.
     S4's system controls and W-A's phone module are also built through here. -->

- [ ] Migrate: intellihide sensing → engine (hyprland collector)
- [ ] Migrate: window-pills fully engine-driven (if not S2's pick)
- [ ] Migrate: notifications box ← engine notifications collector
- [ ] Migrate: clipboard box ← selection/bridge collectors where it fits
- [ ] New module: notes (surface stub exists — pencil button)
- [ ] New module: media (collector + provider exist, needs a surface)
- [ ] Refine per-module when picked: each becomes its own checklist against
      the definition-of-done in optionsmodules.md
